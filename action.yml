name: 'Action-DingDing-Notify'
description: 'Send dingding message. Run on any operating platform, such as Windows, Linux, Mac supported by GitHub'
author: 'yangxiaomin'
branding:
  icon: 'message-circle'
  color: 'red'
inputs:
  token:
    description: 'DingDing bot access-token'
    required: true
  body:
    description: 'Post Body to send'
    required: true
runs:
  using: 'node12'
  main: 'lib/main.js'
